<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aminmemariani.apps.bmicalculator">
    <application
        android:name=".appconfig.AppConfig"
        android:allowBackup="true"
        android:icon="@drawable/bmi"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/bmi_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="DATABASE"
            android:value="bmicalc.db"/>
        <meta-data
            android:name="VERSION"
            android:value="1"/>
        <meta-data
            android:name="QUERY_LOG"
            android:value="true"/>
        <meta-data
            android:name="DOMAIN_PACKAGE_NAME"
            android:value="com.aminmemariani.apps.bmicalculator.dataModel"/>

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".History"></activity>
    </application>
</manifest>